<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">
        <h4 class="navbar-brand">Blogify</h4>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/users/">Home</a>
                </li>
                <% if (locals.user || locals.edit) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/blog/new-blog">Add Blog</a>
                    </li>
                    
                    <a class="nav-item">
                        <% if (locals.user) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/blog/portfolio/<%= user._id %>">Your Blogs</a>
                            </li>
                            <a class="nav-link"><%= user.fullName %></a>
                        <% } else{%>
                            <a class="nav-link"><%= blog.createdBy.fullName %></a>
                            <%}%>
                        
                    </a>
                    <a class="btn btn-primary" href="/users/logout" role="button">Logout</a>
                    <% } else{%>
                        <li class="nav-item">
                            <a class="nav-link" href="/users/signup">Create Account</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/users/signin">Signin</a>
                        </li>

                        <%}%>


            </ul>
        </div>
    </div>
</nav>

<% if (locals.Error) { %>
    <div class="alert alert-danger" role="alert">
        <%= locals.Error %>
    </div>
    <% } %>